import { useAuth0 } from '@auth0/auth0-react';
import React from 'react';

const Home = () => {
    return (
        <div className="flex items-center justify-center py-10">
            <h1 className='text-4xl font-bold text-center'>Welcome to CIRCUITRY.AI</h1>
        </div>
    );
}

export default Home;
